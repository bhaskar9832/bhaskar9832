name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Generate snake animation
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: bhaskar9832
          outputs: |
            dist/github-snake.svg?color_snake=%23FACC15&color_dots=%23222A35,%23C084FC,%233B82F6,%23FDE047,%23CA8A04
            dist/github-snake-dark.svg?palette=github-dark&color_snake=%23FACC15&color_dots=%23222A35,%23C084FC,%233B82F6,%23FDE047,%23CA8A04

      - name: Push snake animation to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
